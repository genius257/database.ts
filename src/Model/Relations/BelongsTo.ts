import Relation from "./Relation";

export default class BelongsTo extends Relation {
    /*
    public constructor(query: Builder, child, foreignKey, ownerKey, relationName) {
        //
    }
    */

    public addConstraints(): void {
        //
    }
}
